---
- name: read_config
  ansible.builtin.slurp:
    src: "~/config"
  register: config_output
  changed_when: true
  notify: print_config

- name: print_config
  ansible.builtin.debug:
    msg: "{{ config_output.content | b64decode }}"
